class DrageEvent {
    constructor(node) {
        this.rootNode = node;
        // 添加可拖拽的属性
        this.holdTime = 0;

        // 添加事件
        this.rootNode.addEventListener('mouse-enter', this.onDrageStart);
        window.addEventListener('touch-up', )
    }

    onDrageStart(event) {
        this.holdTime = 
        if (event.keydown) {
            // 
        }
    }

    DragMove(e) {
        e.transfer = 
        
    }

    onDragEnd() {

    }
}


ondragstart
